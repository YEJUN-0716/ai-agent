# F-Score 대형주 재측정 — 자를 길게, 편향 없이 다시 만들었다

구간 2017-09-01 ~ 2026-08-07 · 진입 `filed`+1 종가 · 252거래일 보유 · 고BM 5분위 ∩ F>=7 롱온리 · 판정 6bp.
유니버스: **대형주 월별 패널**(시총 순위 1~1,000위 = 러셀1000 규칙, 상장폐지 포함) — 이 창의 구성종목 2340개 중 조정 일봉이 있는 2328개. 종목의 키는 티커가 아니라 `"CIK:티커"` 다. 예전 유니버스는 **손으로 적은 현재상장 279종목**이었고, 그 파일이 스스로 "생존자 편향이 있다"고 적어둔 물건이었다.
사전 등록: `docs/superpowers/specs/2026-08-16-largecap-repanel-design.md` (항목·문턱·보유·분위·비용은 `2026-08-14-fscore-design.md` 그대로). **바뀐 건 유니버스와 창 둘뿐이다.**

> **새 데이터로 하는 새 검정이 아니다.** 2020-03~2026-08 의 답은 이미 봤다(`2026-08-14-fscore.md`,
> `2026-08-14-fscore-sealed.md`). 처음 보는 건 **2017-09~2020-03 의 2.6년, 창의 29%** 뿐이다.
> 이 사전 등록이 묶는 것은 데이터의 새로움이 아니라 **결정 규칙**이다.

> **봉인 구간은 없다.** 이미 본 구간을 봉인이라 부르면 거짓말이고, 빼놓으면 선택적
> 보고다. 그래서 전부 넣었다(설계서 6.1).

## 판정: **검출력 부족 — 미측정** (①X AND ②△)

| | 무엇 | 통과선 | 실측 | |
|---|---|---|---|---|
| ① 단면 | 8항목 F-Score 252일 선도수익 IC (전 유니버스) | 날짜 블록 부트스트랩 **t >= +2** (단측) | 평균 IC -0.0141 · t=-0.74 (단면 94개) | X |
| ② 포트폴리오 | 같은 노출 매수보유 대비 초과 연수익 | 부트스트랩 95% 하한 > 0 | +3.62%p · 95% [-8.12, +17.99] | △ |

**하나만 통과하면 실패다.** 이 저장소는 ①만 보고 다섯 번 속았다.

**옛 대형주 측정의 ①은 IC −0.0801 · t=−4.04 였다**(`2026-08-14-fscore.md`). 항목도 통과선도 같은데,
손으로 적은 현재상장 279종목을 **생존자 편향 없는 월별 1,000종목**으로 바꾸자 -0.0141 · t=-0.74 로 사라졌다.
그 −4.04 는 **신호가 아니라 유니버스가 만든 값이었을 가능성이 크다.** 창과 유니버스가 같이 바뀌었으므로
어느 쪽인지는 참고 행(`2026-08-16-fscore-largecap-oldwin.md` — **옛 창** × 새 유니버스)이 답한다.

### 상폐가 어디로 들어왔나 — 재기 전에 막아둔 자리 (설계서 5.1·5.2)

- **벤치마크는 매월 동일가중 리밸런스**다. 그달 구성종목을 동일가중으로 들고,
  상폐 종목은 마지막 거래일 종가에 청산해 다음 리밸런스에서 재분배한다. 예전 한 줄(`시작·종료일에 둘 다 값이 있는 종목만`)을 그대로 썼으면 기준선이
  **"안 죽은 것만 산 줄"** 이 됐다.
- **보유 중 상폐 471건**(고BM ∩ F>=7 바구니 안 6건)은 버리지 않고 마지막 거래일 종가에 청산했다. 예전 코드는 창에 NaN 이 있으면
  그 거래를 뺐다 — 롱온리에서 **손실만 골라 없애는 필터**다.
- **이 청산가는 낙관 쪽으로 틀린다.** 파산 종목은 거래정지 전에 이미 흘러내리고
  정리매매 가격은 더 낮다. 통과가 나오면 이 자리를 먼저 다시 봐야 한다.

## 검출력 — 판정을 보기 전에 낸 값 (2026-08-16 설계서 1절)

| | 값 | 한계 | |
|---|---|---|---|
| **MDE (연 %p, 실제 두 줄)** | **13.05** | <= 10 | X |
| MDE 참고 (기준선 줄만) | 12.12 | 참고 | |
| 평균 자리 수 (전 구간) | 16.40 | >= 5 | O |
| 보유일 평균 자리 수 | 16.66 | 참고 | |

MDE 는 **실제 두 줄의 초과 연수익 95% 구간 반폭**이다. `mde_pp` 는 반폭만 반환하고
점추정을 반환하지 않으므로, 이 값을 내는 동안 효과의 크기도 부호도 안 보인다.
둘째 줄(기준선 줄만으로 낸 참고값)은 2026-08-16 이전에 게이트로 쓰던 값이다 — 대형주에서는
9.29 를 찍어 통과시켰는데 실제는 11.94 였고(작게), 소형주에서는 18.70 인데 실제는 16.49 였다
**이 측정은 ②를 판정할 힘이 없다.** 통과도 실패도 아니라 미측정으로 적는다 — 재기 전에 정한 규칙이고, 결과를 보고 만든 변명이 아니다.

## 다섯 줄 — 설계서가 항상 같이 내라고 한 것

| 줄 | 연수익 | MDD | 비고 |
|---|---|---|---|
| 전략 (고BM ∩ F>=7, 6bp) | +14.2% | -51.4% | 평균 노출 98% · 거래 146건 |
| 같은 노출 매수보유 | +10.6% | -35.0% | ②의 기준선 |
| 원본 매수보유 100% | +10.8% | -35.6% | 통과선은 아니지만 빼지 않는다 |
| 위약 (같은 달 안에서 점수 섞음) | +15.0% | -43.9% | 노출 98% · 거래 254건 |
| BM 스크린 없는 F>=7 | +12.9% | -41.1% | 노출 100% · 거래 2936건 |

**위약 줄을 먼저 읽어야 한다.** 판정 줄의 점추정이 위약과 구별되지 않으면 그건 신호가
아니라 구성이다 — quant_pit ②에서 실제로 그랬다. BM 스크린 없는 줄은 스크린이 무슨
일을 하는지 보는 줄이고, 문헌형이 아니므로 통과선이 아니다.

## 비용 스윕 (왕복 bp, 진입일 전액)

| | 0bp | 6bp | 20bp | 40bp |
|---|---|---|---|---|
| 전략 연수익 | +14.2% | +14.2% | +14.0% | +13.8% |
| 초과 %p (95% 하한) | +3.7 (-8.1) | +3.6 (-8.1) | +3.5 (-8.2) | +3.3 (-8.4) |

## 참고 — 21일 IC (통과선 아님)

평균 IC +0.0052 · t=+0.61 (단면 105개).
quant_pit(+2.24) · chart(+0.94) · ict(+0.98) 과 **같은 자로** 나란히 세우려고 내는 줄이다.
가설의 지평은 252일이므로 판정은 252일로 한다.

## 점수 분포

| F-Score | 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|---|
| 분기 건수 | 4 | 65 | 403 | 1172 | 3246 | 5954 | 5911 | 3870 | 981 |

8항목을 다 채운 종목 998/2328. 나머지는 은행·보험·리츠로,
유동자산/부채를 구분한 대차대조표를 안 낸다. **결손이 아니라 정의역이다** — Piotroski
원 논문도 금융업을 제외했다(설계서 2.2).

## 이 자의 한계 — 재고 나서 알게 된 것

- **①의 유효 블록이 7.8개뿐이다.** 단면 94개를 블록 12(=252거래일 = 지평 하나)로 재표본하므로 사실상 독립 표본이 그 정도다.
  t=-0.74 를 소수점까지 믿으면 안 된다 — 부호와 자릿수까지가 이 자가 말할 수 있는
  전부다. 지평이 252일인 IC 를 4~5년 구간에서 재면 구조적으로 그렇게 된다.
- **전략 줄은 사실상 2018년부터다.** 고BM 분위는 직전 250일 안에 이미 공시된 값들과만 비교하는데(look-ahead 방지), 그 풀이 20건을 채우려면 가격 패널이
  시작하고 한참 지나야 한다. 해마다 표의 첫 해가 +0.0% 인 건 성과가 아니라 **자리가
  없었다는 뜻**이다.
- **거래 146건은 얇다.** 자리 수 하한(평균 5)은 통과했지만 그건 252일 보유가 겹쳐서 채운 값이다. 독립한 진입은 그보다 훨씬 적다.

이 세 줄은 판정을 바꾸지 않는다. **다음에 같은 자를 쓸 때 미리 알고 쓰라고 적는다.**

## 이 측정이 안 한 것

- **2017-08 이전은 못 잰다.** 웨이백 `company_tickers.json` 첫 스냅샷이 2017-08-28 이고,
  그 전에 죽은 종목은 어느 스냅샷에도 없다. **창의 천장이라 여기서 미측정이 나오면**
  **대형주 노선은 닫힌다**(설계서 10절).
- **마이크로캡 미측정.** 시총 순위 1~1,000 위라 하한 중앙값이 $6.70B 다 — 실제 S&P 500
  하한($5~8B)과 겹친다. F-Score 효과가 가장 크다고 알려진 구간은 여기 없다.
- **월별 구성 변경을 거래에는 안 넣었다.** 벤치마크만 매월 리밸런스고, ①과 전략은 이 창에
  한 번이라도 든 종목을 정의역으로 쓴다.
- **재활용 티커의 이전 주인 6종목을 패널에서 뺐다.** 시세 제공자는 심볼로
  **현재 주인의 연속 이력**을 준다. 죽은 회사의 봉을 구할 방법이 없어 통째로 뺐고,
  **그만큼 상폐가 덜 들어 있다(낙관 쪽)**.
- **참고 행은 따로 낸다** — 옛 창 × 새 유니버스 `2026-08-16-fscore-largecap-oldwin.md`.
- **F8(매출총이익률 개선) 미사용.** `GrossProfit` 커버리지 33.0% 로 `MIN_COVERAGE`(70%)
  미달이라 재기 전에 뺐다. 9항목 문헌 재현은 이 재료로는 불가능하다.
- **봉인 없음.** 2025-01~ 은 이미 열어 발행한 구간이라 창에 그대로 넣었다.
- **자리·현금 제약은 하한(3)뿐.** ②를 통과한 뒤에 붙인다 — 못 하면 붙일 이유가 없다.

## 해마다

| 연도 | 전략 (6bp) | 매수보유 | 매수보유 × 98% 노출 |
|---|---|---|---|
| 2017 | +6.2% | +5.7% | +5.6% |
| 2018 | -5.2% | -6.4% | -6.2% |
| 2019 | +10.8% | +26.4% | +25.8% |
| 2020 | +28.5% | +13.4% | +13.1% |
| 2021 | +19.6% | +18.2% | +17.8% |
| 2022 | -2.7% | -14.1% | -13.7% |
| 2023 | +22.8% | +15.5% | +15.2% |
| 2024 | +33.8% | +13.1% | +12.8% |
| 2025 | +15.6% | +16.0% | +15.6% |
| 2026 | +3.2% | +14.4% | +14.1% |

분기 점수 21606건 · 거래 가능 12514건 · 고BM ∩ F>=7 146건. 부트스트랩 2000회 · 시드 20260814.
재현: `python scripts/measure_fscore.py largecap` · 산수 점검 `... selftest`
