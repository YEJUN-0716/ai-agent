# Python
__pycache__/
*.py[cod]
*.pyo
.env

# DART 캐시 (매일 자동 생성, 커밋 불필요)
dart_fund_cache.json
dart_corp_map.json

# 기타 런타임 생성 파일
*.log

# 개인 관심종목 (로컬 전용, 사용자마다 다름 — GH Actions 관리 대상 아님)
watchlist.json

# 가격 패널 캐시 (GitHub Actions cache로 공유, 커밋 대상 아님)
data/
.pytest_cache/

# 로컬 검증 실행이 만드는 빈 PIT DB — 실제 데이터는 워크플로가 생성·커밋한다
pit_fundamentals.db
krx_listing_cache.json
