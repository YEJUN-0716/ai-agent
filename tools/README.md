# tools/

실제로 동작하는 Python 스크립트. 하나의 툴은 하나의 일만 잘 합니다.
API 호출, 데이터 변환, 리포트 생성처럼 예측 가능하고 테스트 가능한 로직만
여기에 둡니다 (판단이 필요한 로직은 워크플로/Claude가 담당).

새 도구를 만들 땐 [`_template.py`](./_template.py)를 복사해서 시작하세요.

## 규칙

- 새 도구를 만들기 전에 기존 도구 중 재사용할 게 없는지 먼저 확인합니다.
- 시크릿은 `.env`에서만 읽습니다. 코드에 하드코딩하지 않습니다.
